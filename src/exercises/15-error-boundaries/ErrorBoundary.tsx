// Ejercicio 15 — Error Boundaries
// Consigna completa en src/exercises/README.md

// TODO: ErrorBoundary de clase, con estado propio (por ejemplo
// { hasError: boolean }). Implementá getDerivedStateFromError y/o
// componentDidCatch, y renderizá una UI de fallback cuando hasError
// sea true.

// Los Error Boundaries deben ser componentes de CLASE — no existe
// (todavía) un equivalente con hooks para esto.

// TODO: un componente que falle a propósito (por ejemplo, un contador
// que tira un error si supera cierto valor), para probar que el
// boundary lo atrapa sin tumbar el resto de la app.
