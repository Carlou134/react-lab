// Ejercicio 05 — useMemo y useCallback
// Consigna completa en src/exercises/README.md

// TODO: generá una lista de al menos 1000 elementos (podés hacerlo con
// Array.from({ length: 1000 }, (_, i) => ({ id: i, ... }))).

// TODO: escribí una función de filtrado/ordenamiento artificialmente
// lenta (por ejemplo, con un bucle que haga trabajo extra por cada
// elemento) y envolvela en useMemo con las dependencias correctas.

// TODO: agregá un estado no relacionado (un contador, por ejemplo) y
// verificá con el Profiler que actualizarlo NO dispara el recálculo.

// TODO: si le pasás una función a un componente hijo memoizado,
// envolvela con useCallback.

export function ExpensiveList() {
  // TODO: implementar
  return null;
}
