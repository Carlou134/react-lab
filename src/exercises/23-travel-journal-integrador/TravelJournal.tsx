// Ejercicio 23 — Proyecto Integrador: Bitácora de Viajes (Zustand + React Query)
// Consigna completa en src/exercises/README.md

// TODO: antes de escribir código, completá este comentario explicando
// por qué cada dato va en Zustand o en React Query:
//
// - persona activa y su rol (mock)  -> ¿por qué?
// - idioma de la interfaz           -> ¿por qué?
// - panel de filtros desplegado     -> ¿por qué?
// - listado de entradas             -> ¿por qué?
// - detalle de una entrada          -> ¿por qué?

// TODO: store de Zustand — persona activa (rol 'editor' | 'lector'),
// idioma ('es' | 'en') y panel de filtros. Idioma y panel persistidos
// con el middleware persist (repasá 09-zustand y 10-state-persistence).

// TODO: queries y mutaciones de React Query para las entradas —
// listar (con filtro por etiqueta), detalle, crear, editar, borrar.
// Usá una fábrica de query keys como en el ejercicio 22 e invalidá lo
// que corresponda tras cada mutación. Solo el rol 'editor' ve los
// botones de crear/editar/borrar.

export function TravelJournal() {
  // TODO: implementar. Con una lista de entradas, un detalle, los
  // botones de edición y los controles de idioma y panel funcionando
  // (y persistiendo) alcanza.
  return null;
}
